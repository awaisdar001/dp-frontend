

1. Create a virtual env & make sure you have `virtualenv` installed
	`virtualenv venv`

2. Enable VENV => source venv/bin/activate

3. Install requirements
	`make requirements`

4. Install Node Modules this will create node modules
	`make static`

4. Run Djnago server
	`make run`

5. Run Webpack Watcher
	`npm run hot`


